# Test fixture: project with intentionally failing contracts
# Author: Andrew Yates <andrewyates.name@gmail.com>

[package]
name = "failing_project"
version = "0.1.0"
edition = "2021"

# Empty workspace table to exclude from parent workspace
[workspace]

[dependencies]
trust-wp = { path = "../../../crates/trust-wp" }
